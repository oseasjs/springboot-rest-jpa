spring:
  config:
    activate:
      on-profile:
        - test
  datasource:
    url: "jdbc:h2:mem:blog_test;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL;INIT=CREATE SCHEMA IF NOT EXISTS blog\\;CREATE SCHEMA IF NOT EXISTS audit"
    username: sa
    password: sa
    driverClassName: org.h2.Driver